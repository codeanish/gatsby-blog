import React from 'react'
import Layout from '../components/layout';

const About = () => {
    return (
        <Layout>
            <h1>About</h1>
            <p>This is testing whether or not the continuous deployment to Netlify is correctly set up.</p>
        </Layout>
    )
}

export default About;